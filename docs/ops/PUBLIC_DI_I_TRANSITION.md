# Public `di.i` Transition (Archived)

This plan is archived.

The repo currently uses:

- `dob-0/di.iiii` as the public, active, deploy-connected source of truth
- `dob-0/di.i` as a hidden/inactive legacy mirror

The old transition model (promoting `di.i` as the main public home while keeping `di.iiii` private) is not active.

If a future migration is needed, create a new transition plan from the current state instead of reusing this archived one.
